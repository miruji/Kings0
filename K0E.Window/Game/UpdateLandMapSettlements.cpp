#include "../Window.h"

// Game: Update land map settlements 
void Window::UpdateLandMapSettlements(bool NewStatus)
{
	/*
	for (size_t i = 0; i < Game1->Castes.size(); i++)
	for (size_t j = 0; j < Game1->Castes[i]->Settlements.size(); j++)
	{

		Game1->Castes[i]->Settlements[j].Drawing = NewStatus;
		bool Status = LandMap1->BlocksVector[
			size_t(Game1->Castes[i]->Settlements[j].Y/32)
		][
			size_t(Game1->Castes[i]->Settlements[j].X/32)
		].SettlementDrawing = !NewStatus;
	}
	*/
}